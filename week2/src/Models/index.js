//to reference index we just type folder name so Models here
const Person = require("./Person");
const Student = require("./Student");
const Teacher = require("./Teacher");



module.exports = {
    Person,
    Student,
    Teacher,
}